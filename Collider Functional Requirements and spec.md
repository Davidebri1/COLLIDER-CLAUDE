Functional Requirements 

Title: Give me one that's available and excellent. and come up with a list of good alternatives that are available.
Purpose: AAA visual quality. Industry leading and offering a 2026 worthy design aesthetic; The app is for users to give ordinary people a full picture when using ai models. and to consistently, and aggressively reduce their cognitive load by removing friction, decisionmaking between trivial options, fr5t it provides easy validation of responses/generation by allowing up to 9 models to be shown in view per model category. this allows easy compare and contrast of the message preview areas of each. Card view lets users dive in to any model with a click for deeper review. The main purpose of the app is to reduce friction and cognitive effort. Cognitive effort is objectively negative and removing it is objectively valuable. so every decision is made in service of that. proactive value. providing what the user will logically need before they have time to look for it or ask for it. this is how to provide it. by forcing intuitive design into every decision. no extra button presses. 

Develop a mobile app (native, not web app) that allows you to query multiple ai models at once and view their responses in a grid. from the grid, you can click on any one to access a standard card view and continue persistant conversation (account persistence and local). i want this app to have a general featureset that applies to all of its models across its various tabs. its featureset should be derived from industry leaders and consolidated as an offering for all of my app's models. This means i dont want users to leave my app because of another app's featureset. i dont want grok users to use my app, realize i dont have an imagine market equivalent, and go back to grok. i want full functional quality. of course this will be price gated. free users get 20 messages /day(regardless of number of models selected per message). and no access to media gen models (image, video, audio) or coding models(agentic with customization). i want this full feature set implemented. 

login with google, apple, email and password. guest mode default. no login screen friction. account management located in menus. with logout button. 

Killer App Feature 1:
Immersive Imagery:
i want the background image to be changeable by a theme switcher. and uninterrupted by the cards or other permanent UI features. so cards must be transparent. the background image must also show in detail card view.  it should be a seamless transition from grid view to card view. 



Killer App Feature 2:

Smart Gen Suite:
smart gen tools are a summary list of features pulled from other competitor apps, with a twist: they can be autogenerated based on detailed context extraction. you go through a court case with any of the models, detailing facts, and asking questions, smart gen will automatically create project for you for it, unless one already exists.example: if you ask for deadlines, the model will look them up and present them to you, and also load it as a reminder and task for you. reminders with timedate entries autotmatically get added to your calendar as well. like google calendars (i want its full functionality), filters exist for model, project, and other key relevant fields.  any documents generated regarding the case like a tmeline or statement, will be created as artifacts automatically and saved in the project for this case. as you can understand, smart gen wont crate duplicates within a category, project, model. they can also be freely converted from one type to another and have custom fields. they can also be embedded in one another. so artifacts can be automatically attached to files or reminders or tasks or projects, and likewise reminders can be automatically attached to projects, artifacts, memories, etc.
the examples provided are not exhaustive. recruit a subagent if you need that can help you implement a robust solution that catches myriad of obvious necessitated captures and updates. dont forget the updates. closing task, updating data in tasks. you can make these both optional toggles.  

smart gen Suite tools:
projects
tasks
reminders (with calendar)
memories
artifacts


History:
History should be handled the same as the conventional way in all AI apps. autoarchive and autoname and timedate stamped (elegantly)upon "new conversation", clearing the existing conversation of the selected model(s) only. the archive will be stored in the history pane which will always be on the left in both card view and global view. clicking on an entry in that table will load that history and store the current selected model(s) history. 

i want a search feature obviously with filters for type (conv history, smart gen tools,etc.) and by model. 

and i want a consensus feature that is a full screen drawer that can be opened that first shows the number of selected models that agree at the top, colorded red to green based on alignment, then just below,a brief summary of the consensus result, in blue soft glowing text. then below that the dissenting map. a 2.5d or 3d galactic mapping, a constellation, of dissenting views. each dissenting view, not part of the consensus, has a 3d or 2.5d sphere attached to their one line dissent summary. the model name is shown in abbreviation by the sphere. these will look like stars in a galaxy. so black background. the consensus summary will be centered horiontal, with space left and right. below the dissenting views will be located vertically based on their agreement with the consensus summary. and located horizontally, based on their collective agreement with other dissenting views. other stars in high disagreement with one another will be on opposite ends horizontally. but their position is a net calculation, so other stars, perhaps with strong affinity to another star, will be grouped closer to that star, and might bring that star closer to center. 

media gen models will require credits to use. these will be accessible by pro and elite tier users. credits will have daily and monthly limits. maybe weekly too. bbut pro and elite members get unlimited use of general tabs (chat only models, accessible fully by free users at a metered rate). these must be fully working and feature appropriate for the modal. so all of the features should be sorted by the result chat interface, in both views. Be sure to include on press action menus for generated or imported content and for messages, wih the relevant standard options. 

i want grok's imagine market in full.

Aesthetic:
Follow the included design system.. 

Each model category is independent. and each will have the full relevant featureset and chat interface. with proper settings and parameter options. and proper generation management and interaction. action bars on click for each selected item that are context appropriate for their type. image generation models for instance(not exhaustive) should have at minimum: generation column to hold recent generations, generations as deliverables in chat window, prompt queue for context injuection for the message, action bar with minimum: view (exploded view). use as context(multiple allowed), use as source (1 only), delete, share, save to collection (in app), favorite icon, delete and anymore needed. this isnt unique. this is standard. use the proper imports to avoid unnecessary work. this goes for the entire app. no one by one development. create an element, a family, a type, and simply change its parameters. you dont need to one by one make 9 model cards. only make one, a template, and simply change the name and api and any other parameters you need to. same goes for ui elements and everything else. market templates exist. media gen templates exist. collection templates exist. 


Important Guidance:
Focus on quality. 


output must be logically implemented to serve a purpose. maintain the logical chain through to output. if you lose it, dont assume, just ask me to help you understand. and i will tell you and you will be realigned. the chain restored. context is irrelevant.

 actively check against competitors and general conventions to make sure what youre providing is relevant and helpful. 

All menus and UI instances and elements should be safe and should never trap the user. always include a back button, an x button or a swipe away where appropriate. 

allow multiselect and edit and delete of all list items (smart gen suite, conversation histort)


# Collider — Product Spec

 Do not rely on chat history for
requirement recall — update this file when requirements change, and read
from it, not from conversation memory.

## Core concept
Multi-model AI chat app (native, iOS/Android, Expo). Query multiple models at
once, view responses in a grid of cards. Click a card to open a full detail
("card view") for that model. Purpose: consolidate industry-leading features
from competitor apps (ChatGPT, Claude, Gemini, Grok, etc.) so users never
need to leave the app for a feature another app has. Continuation of
conversations is a differentiator (my conversations can be continued,
others cannot). And behind the card grid is a beautiful wallpaper, selectable in the theme switcher, which also sells premium, live wallpapers. This wallpaper is the heart of the app. the soul. so it should not be carelessly rendered or interrupted. it serves to bring an emotional attachment and a feeling to the 

Settings should persist account wide and locally. 

## Categories / tabs
- General, Image, Video, Audio (Music), Coding — each independently scoped:
  own selected models, own conversation/grid state. Selections persist independent of one another. and persist through account persistence.

## Model selection counts (per category) — 

| Category | Free | Pro | Elite |
|---|---|---|---|
| 3.1. General | 8 | 4 | 4 |
| 3.2. Image | 0 | 4 | 4 |
| 3.3. Video | 0 | 4 | 4 |
| 3.4. Audio | 0 | 4 | 4 |
| 3.5. Coding | 0 (2 exist for roster display only) | 4 | 4 |
- Roster is real, curated OpenRouter/Groq catalog IDs (verified against a
  live catalog fetch, not invented) — see `src/models.ts` for the exact list
  and `src/services/chat.ts`'s `ROUTES` for the provider mapping. No Opus, no
  Fable anywhere (the app offers unlimited messages, so per-request cost has
  to stay sane at every tier). Make sure to wire with openrouter key.
- 

-
## Tiers & gating

- Free: 20 messages/day total, regardless of how many models are selected
  per message. No access to media gen (image/video/audio) or coding models.
- Pro / Elite: unlimited use of General (chat-only) tabs.
- Media gen (image/video/audio) requires credits — available to Pro and
  Elite only. Credits have daily, weekly, and monthly limits
- Coding models (agentic, customizable) are Pro/Elite only.

Determine the proper or logical economy for the app. credit awards, when and how often, how much, how much different types of model outputs cost. including consensus if you want to charge users messages or credits for them.

## Grid view (main view / Global View)


- Title bar: as-is.
- Model selector: popup (`Select Models (CATEGORY)` modal).
- Category selector: a dropdown trigger (`CategoryDropdownTrigger` +
  `CategorySelectorModal`), not a ribbon/row of tabs. **Resolved.**
- Model cards: transparent/glass — background image shows through and fills the screen without crop or zoom. This is our permanent engagement layer.
- Text should be abundantly readable without covering up background wallpaper. test against multiple backgrounds.
- Don't obscure background noticeably to enhance text. any selective blockages of a full image always read as obnoxious and restrictive. 
- Card text fills from the top; new replies scroll to the *top* of the
  latest message, not the bottom of it (applies to both the grid mini-cards
  and the full card-detail view — same `onLayout` + `scrollTo(y)` pattern in
  both). **Resolved** — was previously anchoring to bottom / scrolling to end.
- Markdown rendering required
- Prompt bar should be slick and proessional. good font. It should include the minimal features: attachments, styles/adjusters (research, deep research, internet, etc.), voice button, message area (with placeholder text: "Enter message", send button, and anything else you think i missed or competitors have.

## Card (detail) view
- Full standard chat view for a single model.
- Background image (theme) renders here too, same as grid view.
- Right drawer (Smart Gen tools) is local/per-card scope here vs. global in
  grid view.

## Theming & color system
- Background image is swappable via a theme switcher (image swap only —
  themes are not different color schemes, just different background images).
  Glass cards remain glass across all themes.

-- Model identity colors (each model's own accent in `src/models.ts`) and
  category icon colors are exempt from the above — those are intentionally
  varied to distinguish many models/categories at a glance, same logic as
  photos/images being exempt.
- Font: Manrope, loaded as 4 separate static-weight files at your discretion(700/800),
  not one variable font. , Playfair Display 700 + 800 for headers. Always resolve `fontFamily` via
  `fontFamilyForWeight(weight)` (theme.ts) alongside any `fontWeight` — using
  the Regular file with a bold `fontWeight` produces browser-synthesized
  faux-bold (thin/blurry), not a real bold face.

## Drawers
Follow and extend design system to all submenus, buttons, icons, etc. 
- Left drawer: Settings/Account/Usage (utility icons at top) + History as a
  sortable table with conversation date, model(for global view), and datetime (tap header to sort, tap again to flip direction — same
  pattern as Reminders), with Consensus as a tab lip alongside Conversations.
- Right drawer: "Smart Gen tools" — global scope in grid view, local
  (per-card) scope in card/detail view.
-be sure to fit the entire view in the frame. no scrolls.

## Smart Gen Suite
Follow and extend design system

Tools: **Projects, Tasks, Reminders (with full calendar), Memories,
Artifacts.**

- Autogeneration: tools can be auto-created from detailed context extraction
  during conversation. Example: working a case with a model (facts, Q&A) →
  Smart Gen auto-creates a Project for it, unless one already exists for
  that case.
- Deadlines: if the user asks about deadlines, the model looks them up,
  presents them, AND auto-creates a Reminder/Task for them.
- Reminders with a date/time get a scheduled push notification
  (`services/media.ts`'s `scheduleReminder`) at the exact due time.
  **Not implemented**: any alert-offset options (e.g. "5 min before"),
  per-reminder notification toggle, or repeat/recurring alerts — it's a
  single fixed-time notification with no configurability yet.
- Documents generated during a conversation (e.g. a timeline, a statement)
  are auto-saved as Artifacts, attached to the relevant Project.
- No duplicate creation within the same category/project/model — Smart Gen
  must check for an existing match before creating a new one.
- Items are freely convertible between types (e.g. Task ↔ Reminder) and
  support custom fields.
- Items can be embedded/attached to one another in any combination:
  Artifacts → Files/Reminders/Tasks/Projects; Reminders → Projects,
  Artifacts, Memories; etc. Fully cross-linked graph, not a rigid hierarchy.
- **Product principle**: tools should proactively deliver the thing, not ask
  permission first ("I have X, do you want it?"). If the model can think to
  ask, it can provide it — the user was going to say yes anyway, and can
  ignore it if not.
- **List views are full tables, not partial ones.** A form's fields
  naturally become a table's columns — every field the item type defines
  gets a column, sortable (tap header, toggles direction) and filterable,
  not a curated subset. Concretely, per type (`state.tsx`'s field
  definitions):
  - Reminders: title, due, priority, status/progress, tags, project.
  - Memories: content, source model, priority, tags, project.
  - Projects: name, task count/completion, linked items.
  - Artifacts: title, kind, source model, project, last updated.
  **Current gap**: only `RemindersScreen.tsx` has any sortable table
  header, and even it only covers 4 of the type's fields (title/due/
  priority/status) — tags and project aren't columns. Memories, Projects,
  and Artifacts have no table/sort/filter treatment at all, just a plain
  card list.

## Search
- Global search (`InlineSearch.tsx`, docked inline below the grid header,
  not a full-screen modal) with filters by type (conversation history, Smart
  Gen tool type) and model. Streaming/live-filtered as you type; the results
  tray only renders when there's something to show — no "No results found"
  empty-state box for an unfiltered/no-match state. it also works locally. 

## Consensus Full Screen Drawer ("Collide")

Follow the Design System

"Collide" is the button/trigger; "Consensus" is the full-screen result it
opens — same action, two names for two halves of it. One button, one
destination.

Full-screen drawer, opened on demand (costs one message for free-tier users,
same as any other general-chat send — Collide isn't free to trigger). 1-Page Layout
top to bottom:
1. Score (e.g. `4/7`) — its own centered line, red→green colored by
   alignment strength. Not squeezed into a row with other elements.
2. Consensus summary — narrower, centered box below the score, aligned under
   the map's own center column.
3. The dissent map Tab 1 — 2.5D constellation on a black background, given the
   majority of the screen's vertical space (map > score > summary in
   footprint). Center node uses blue/red duality (binary agree/disagree is
   literally what this feature measures). Each dissenting model = a sphere
   with its abbreviated name, vertical position = agreement level, horizontal
   position = force-directed based on mutual agreement with other dissenters.
   Dissenting-view text can render inline next to each sphere (toggleable —
   "LABELS ON/OFF") in addition to always being listed below the map.
- Built from plain View/LinearGradient, but can be `react-native-svg` — SVG
  primitives (Circle/Line/SvgText/RadialGradient) 
4.The Total Map Tab 2 - the same as the dissent map, but with all models. the dissenting views now do not have a line drawn to them. 

#4,  The Total Map is what is shown in the design system. The Dissenting Map is not shown and is not in view. It needs to be added. Create a tab in the dissenting view map area in front of the Total Map. Place the dissentiv view map in this new tab. Dissenting view should now be tab 1

## Collide Consensus preview bar (`CollideBanner.tsx`)
Follow the Design System

Sits above the composer, always rendered — the synthesis is surfaced before
the user has to ask for it. Tapping it opens that full screen drawer.

- Same two-state rule as the full Consensus drawer: **complete** (every
  selected model has replied successfully) or **incomplete**. No partial/in-progress wording ("waiting
  on 2 of 5") — the reason the scope isn't complete doesn't change what's
  true, so it collapses to one word. If a model errors out, highlight it in red(with matching fidelity for the design system and aesthetic) temporarily so the user knows to remove the model card.
- Score badge:  fraction (e.g. `2/3`), colored
  red→green by alignment strength. Renders `–/–` when incomplete.
- Merge the Design system's Collide button (at the bottom of the app in grid view) with consensus above. Incorporate it as a header elegantly in the that box. it's a duplicate button right now. but i think the visual is great. 


## Media Gen Market
- Full Grok-"Imagine"-market-equivalent: browse/generate image/video/audio
  content, remix, insert-as-source/insert-as-context.
- Backed by a real Supabase table (`market_items`) with real offset/limit
  pagination (`fetchMarketItems` in `services/market.ts`) — not a hardcoded
  cap. 
- Credits-gated per the tiers section above.
- Fully generated, with real procedural generations and full feature parity and functionality
- If it doesn't offer the same UX as industry standard, it is not compliant. no exceptions.
- Standard featureset should be included, and with representation for all tab categories


## Wallpapers & Music
- Premium wallpapers are **live wallpapers** (video loops), each sold as an
  **individual purchase** — not unlocked in bulk by Pro/Elite tier alone.
  Price band: **$2.99–$7.99 per wallpaper**. Gating is per-item
  (`state.ownedWallpaperIds`), not a blanket tier check.
- Code Native Purchase integration for purchases
- Each live wallpaper ships with **at least 5 curated music tracks**. One
  mock wallpaper+tracklist (`premium_mock_aurora`, public sample video +
  SoundHelix test tracks) exists end-to-end for testing the purchase→own→
  play pipeline — no real premium video/audio assets exist yet.
- Background audio player: `expo-av`'s `Audio` API
  (`services/musicPlayer.ts`), not `react-native-track-player` — track-player
  needs the same native-dev-client build that IAP does, which this project's
  workflow can't currently produce; expo-av is already a dependency and
  covers every requirement (background playback via
  `staysActiveInBackground`, looping, volume) without adding one.
- A persistent corner volume/mute control (`CornerVolumeControl.tsx`) is
  mounted at the app root — visible on every screen once a track has played,
  single tap mutes, long-press expands play/pause + a volume step control.
- Each track can be toggled on/off in the playlist independent of play/pause
  — toggling off a currently-playing track actually stops it.

## clarifications
- Themes = background image swap only. Do not build a secondary
  color-theme system — glass card styling is constant across themes, and
  there is no user-facing accent-hue picker (see Theming above).
- Regarding free tier general tab message usage: 1 message is 1 message regardless of number of models selected in a single multi-model query.
-Give me a folder link to drop background images into (free themes) and one to drop videos into it (live wallpaper loops premium themes)